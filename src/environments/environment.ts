// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyCf8TNoDqJwedv8I0TudNGJa9cl7KbxyZ0',
    authDomain: 'firechat-56aea.firebaseapp.com',
    databaseURL: 'https://firechat-56aea.firebaseio.com',
    projectId: 'firechat-56aea',
    storageBucket: '',
    messagingSenderId: '269484962387',
    appId: '1:269484962387:web:12907879730796c4'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
